# zbze_ocr

Система обработки и оптического распознавания текста (OCR) для документов на кабардино-черкесском языке.

## Описание

**zbze_ocr** — это комплексное решение для высококачественного распознавания текста из отсканированных документов и PDF файлов на кабардино-черкесском языке. Проект включает:

- 🔧 **CLI инструменты** для предварительной обработки изображений и PDF
- 🤖 **Apache Airflow DAGs** для автоматизации процессов обработки
- 📚 **Кастомную модель Tesseract** для кабардино-черкесского языка
- 🌐 **Django REST API** для управления результатами OCR
- 🚀 **Async веб-загрузчик** (aiohttp + WebSocket) для обработки файлов в реальном времени
- ✨ **Computer Vision модули** для продвинутой обработки изображений
- 📓 **Jupyter Notebooks** для исследований и экспериментов

## Основные возможности

### Предобработка документов

- **Разделение PDF на изображения** с высоким качеством
- **Разделение разворотов книг** на отдельные страницы
- **Автоматический поворот** изображений
- **Удаление шумов и артефактов** сканирования (unpaper)
- **Применение фильтров** для улучшения читаемости:
  - Яркость и контрастность
  - Резкость
  - Сглаживание
- **Стандартизация размеров** изображений

### OCR и распознавание

- **Оптимизированная модель Tesseract** для кабардино-черкесского языка
- **OCRmyPDF интеграция** для создания searchable PDF
- **Сравнение результатов** распознавания разных версий
- **Проверка орфографии** с поддержкой кабардино-черкесского языка

### Автоматизация

- **Airflow DAGs** для пакетной обработки
- **Генерация обучающих данных** для улучшения моделей
- **Сравнение языковых моделей** и метрики качества

### Веб-интерфейсы

**OCR Viewer (Django)**
- REST API для доступа к результатам OCR
- Django Admin панель для управления документами
- Модели для хранения PDF, страниц и распознанного текста
- Фильтрация и поиск по результатам
- Экспорт данных через API
- Сериализаторы для JSON представления
- Таблицы для визуализации результатов

**Uploader (aiohttp + WebSocket)**
- Асинхронная загрузка больших файлов (до 1 GB)
- WebSocket для мониторинга процесса обработки в реальном времени
- Управление задачами обработки
- Предпросмотр и скачивание результатов
- Оптимизирован с uvloop для максимальной производительности

### Computer Vision модули

В `cli/logic/cv/` содержатся специализированные модули:
- **detectors.py** - детекторы контуров, текстовых областей
- **filters.py** - набор фильтров для улучшения изображений
- **modifications.py** - трансформации изображений (поворот, масштабирование)
- **drawing.py** - визуализация результатов обработки

## Требования

### Системные зависимости

- **Python 3.10+**
- **Tesseract 5.0+** ([установка](https://tesseract-ocr.github.io/tessdoc/Installation.html))
- **ImageMagick 7.0+** ([установка](https://imagemagick.org/script/download.php))
- **OCRmyPDF** ([установка](https://ocrmypdf.readthedocs.io/en/latest/installation.html))
- **Unpaper** (опционально, для постобработки сканов)

### Python зависимости

Основные пакеты (полный список в `requirements.txt`):

**Обработка документов:**
- `pytesseract` 0.3.10 - Python обертка для Tesseract
- `ocrmypdf` 16.3.1 - OCR для PDF с searchable text
- `pypdf` 4.3.1, `pikepdf` 8.15.1, `PyMuPDF` 1.24.9 - работа с PDF
- `Pillow` 10.2.0 - обработка изображений
- `opencv-python` 4.8.0.76 - Computer Vision

**Веб и API:**
- `Django` 5.0.6 + `djangorestframework` 3.15.1 - REST API
- `aiohttp` 3.8.6 + `uvloop` 0.19.0 - асинхронный веб-сервер

**Автоматизация:**
- `apache-airflow[celery,postgres,redis]` 2.7.2 - оркестрация workflows

**Проверка орфографии:**
- `nltk` 3.8.1 - NLP библиотека
- `tokenizers` 0.15.1 - токенизация текста
- `pyahocorasick` 2.0.0, `marisa-trie` 1.1.0 - эффективный поиск в словарях
- `scikit-learn` 1.3.2, `fuzzywuzzy` 0.18.0 - нечеткое сравнение

**Анализ данных:**
- `pandas` 2.0.3, `numpy` 1.24.4
- `matplotlib` 3.8.2, `seaborn` 0.13.2 - визуализация
- `jupyter` 1.0.0 - интерактивные notebooks

**Разработка:**
- `black` 23.9.1, `isort` 5.13.2 - форматирование кода
- `pysnooper` 1.2.0 - отладка

## Быстрый старт

### 1. Установка

```bash
# Клонирование репозитория
git clone https://github.com/yourusername/zbze_ocr.git
cd zbze_ocr

# Создание виртуального окружения
python3.10 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установка зависимостей
pip install -r requirements.txt
```

### 2. Установка языковой модели Tesseract

```bash
# Копирование обученной модели для кабардино-черкесского языка
sudo cp tesseract/trained_data/kbd_finetuned.traineddata /usr/share/tesseract-ocr/5/tessdata/kbd.traineddata

# Проверка установки
tesseract --list-langs | grep kbd
```

### 3. Обработка первого документа

```bash
cd cli

# Поместите ваш PDF файл в data/pdfs/
# Например: data/pdfs/my-document.pdf

# Запуск полного цикла обработки
make all PDF_FILE=my-document.pdf
```

Результаты будут в `data/pdf_processed/my-document.pdf/`:
- `processed_my-document.pdf` - обработанный PDF, готовый для OCR
- `original_my-document.pdf` - копия оригинала

### 4. Запуск OCR

```bash
cd data/pdf_processed/my-document.pdf/

# Через OCRmyPDF
ocrmypdf -l kbd --force-ocr --clean --deskew \
  --unpaper-args '--layout single' \
  processed_my-document.pdf \
  my-document.ocr.pdf \
  --sidecar my-document.txt \
  --tesseract-config ../../../tesseract/kdb.base.config.txt
```

Результат:
- `my-document.ocr.pdf` - PDF с распознанным текстом
- `my-document.txt` - извлеченный текст

## CLI команды

Проект предоставляет набор CLI инструментов для пошаговой обработки:

```bash
# Просмотр всех доступных команд
python -m cli --help
```

### Основные команды

| Команда | Описание |
|---------|----------|
| `split-pdf-to-jpeg` | Конвертация PDF в JPEG изображения |
| `split-book-layout` | Разделение книжных разворотов |
| `unpaper-img` | Постобработка сканов (удаление шумов) |
| `rotate-img` | Автоматический поворот изображений |
| `apply-filters` | Применение фильтров (яркость, контраст, резкость) |
| `standartize-img` | Стандартизация ширины изображений |
| `join-jpeg-to-pdf` | Объединение изображений в PDF |
| `run-tesseract-for-page` | Запуск Tesseract для отдельной страницы |
| `compare-texts` | Сравнение результатов OCR |
| `box-processing` | Обработка областей распознавания |

## Структура проекта

```
zbze_ocr/
├── cli/                      # CLI инструменты для обработки
│   ├── logic/               # Логика обработки изображений
│   │   ├── cv/             # Computer Vision модули
│   │   │   ├── detectors.py    # Детекторы контуров и областей
│   │   │   ├── filters.py      # Фильтры изображений
│   │   │   ├── modifications.py # Трансформации
│   │   │   └── drawing.py      # Визуализация
│   │   ├── split_book_layout.py
│   │   ├── rotate_img.py
│   │   └── ...
│   ├── Makefile            # Автоматизация обработки
│   └── *.py                # CLI команды
│
├── dags/                    # Apache Airflow DAGs
│   ├── src/                # Общий код для DAGs
│   │   ├── spell_checker/  # Проверка орфографии
│   │   ├── image_generator.py
│   │   ├── lang_compare.py
│   │   └── ...
│   ├── 00_image_generate.py
│   ├── 00_train_tesseract.py
│   ├── 01_pdf_processing.py
│   └── 02_langs_compare.py
│
├── tesseract/              # Tesseract конфигурация
│   ├── trained_data/       # Обученные модели OCR
│   │   ├── kbd.traineddata
│   │   └── kbd_finetuned.traineddata
│   ├── tesstrain/          # Данные для обучения
│   ├── txt_compare/        # Примеры сравнения (001-007)
│   ├── kdb.base.config.txt # Конфигурация Tesseract
│   └── ocrmypdf_kbd.Dockerfile
│
├── web/                    # Веб-приложения
│   ├── ocr_viewer/        # Django REST API
│   │   └── app/
│   │       ├── ocr/       # Основное приложение
│   │       │   ├── models.py
│   │       │   ├── serializers.py
│   │       │   ├── views/
│   │       │   ├── management/
│   │       │   └── services/
│   │       ├── api/       # REST API endpoints
│   │       └── setting/   # Настройки Django
│   └── uploader/          # Aiohttp + WebSocket загрузчик
│       ├── app.py         # Главное приложение
│       ├── ws.py          # WebSocket обработчик
│       ├── managers.py    # Менеджеры задач
│       ├── views.py       # HTTP views
│       ├── templates/     # Jinja2 шаблоны
│       └── Dockerfile
│
├── notebooks/              # Jupyter notebooks
│   ├── 00_clean_text.ipynb
│   ├── 02_generate_image.ipynb
│   ├── 04_compare_two_lang_by_conf.ipynb
│   ├── 09_spell_check.ipynb
│   └── img_prepocessing/  # Эксперименты с обработкой
│
├── docs/                   # Документация
│   ├── airflow/           # Airflow setup и DAGs
│   ├── tesseract/         # Tesseract документация
│   ├── web/               # Веб-приложения
│   ├── screenshots/       # Скриншоты для документации
│   ├── main.md            # Основная документация
│   └── todo.md            # Roadmap проекта
│
├── airflow.cfg.template    # Шаблон конфигурации Airflow
├── webserver_config.py     # Конфигурация Airflow webserver
├── pyproject.toml          # Конфигурация black/isort
└── requirements.txt        # Python зависимости
```

## Документация

Подробная документация доступна в директории `docs/`:

- **[docs/main.md](docs/main.md)** - Полное руководство по установке и использованию
- **[docs/todo.md](docs/todo.md)** - Roadmap проекта и планы развития
- **[docs/tesseract/readme.md](docs/tesseract/readme.md)** - Работа с Tesseract и OCRmyPDF
- **[docs/airflow/](docs/airflow/)** - Настройка и использование Airflow DAGs
- **[docs/web/](docs/web/)** - Документация веб-приложений
- **[docs/screenshots/](docs/screenshots/)** - Примеры и скриншоты
- **[docs/context_for_llm.md](docs/context_for_llm.md)** - Утилиты для генерации контекста

## Jupyter Notebooks

В директории `notebooks/` содержатся исследовательские ноутбуки:

**Обработка текста:**
- `00_clean_text.ipynb` - Очистка и нормализация текста
- `00_collect_text.ipynb` - Сбор текста из различных источников
- `00_extract_collocation.ipynb` - Извлечение коллокаций

**Генерация обучающих данных:**
- `02_generate_image.ipynb` - Генерация синтетических изображений текста
- `02_prepare_ground_truth.ipynb` - Подготовка ground truth данных
- `13_prepare_box_for_train.ipynb` - Подготовка box файлов для обучения

**Анализ и сравнение:**
- `04_compare_two_lang_by_conf.ipynb` - Сравнение языковых моделей
- `04_visualize_compare.ipynb` - Визуализация результатов сравнения
- `09_spell_check.ipynb` - Проверка орфографии

**Обработка изображений:**
- `img_prepocessing/split_book_layout.ipynb` - Разделение книжных разворотов
- `img_prepocessing/image_processing.ipynb` - Различные методы обработки
- `img_prepocessing/image_descew_by_text.ipynb` - Выравнивание по тексту

## Использование с Apache Airflow

Проект включает DAG'и для автоматизации процессов:

1. **00_image_generate.py** - Генерация обучающих изображений для тренировки Tesseract
2. **00_train_tesseract.py** - Автоматизированная тренировка модели Tesseract
3. **01_pdf_processing.py** - Массовая обработка PDF файлов через pipeline
4. **02_langs_compare.py** - Сравнение качества разных языковых моделей с метриками

### Настройка Airflow

```bash
# Скопируйте шаблон конфигурации
cp airflow.cfg.template airflow.cfg

# Webserver конфигурация уже настроена в webserver_config.py
# Включает настройки аутентификации и безопасности

# Инициализация БД
airflow db init

# Запуск webserver
airflow webserver

# Запуск scheduler (в отдельном терминале)
airflow scheduler
```

Подробнее см. [docs/airflow/setup.md](docs/airflow/setup.md)

## Использование с Docker

Для OCR с кастомной моделью через Docker:

```bash
# Сборка образа
cd tesseract
docker build -f ocrmypdf_kbd.Dockerfile -t ocrmypdf_kbd .

# Использование
docker run --rm -v $(pwd):/data ocrmypdf_kbd \
  -l kbd --force-ocr input.pdf output.pdf
```

## Примеры использования

### Пример 1: Базовая обработка

```bash
cd cli
make all PDF_FILE=document.pdf
```

### Пример 2: Только разделение разворотов

```bash
python -m cli split-book-layout \
  -d data/pdf_processed/document/00_jpeg \
  -o data/pdf_processed/document/01_layout_splitted
```

### Пример 3: Применение фильтров с параметрами

```bash
python -m cli apply-filters \
  -d data/images/input \
  -o data/images/output \
  -g 0  # Набор фильтров (0-3)
```

## Разработка

### Форматирование кода

Проект использует `black` и `isort` для форматирования:

```bash
# Форматирование
black cli/ dags/
isort cli/ dags/

# Проверка
black --check cli/ dags/
```

Конфигурация в `pyproject.toml`.

### Веб-приложения

**OCR Viewer (Django REST API)**

Полноценное Django приложение с REST API для управления результатами OCR:

```bash
cd web/ocr_viewer/app
python manage.py migrate
python manage.py createsuperuser
python manage.py runserver
```

Функциональность:
- REST API endpoints в `/api/`
- Django Admin панель для управления
- Модели: PDF, Page, OCRText, OCRWord
- Сериализаторы для JSON API
- Фильтрация и поиск
- Management команды для импорта данных

**Uploader (Async веб-загрузчик)**

Асинхронное приложение на aiohttp с WebSocket для загрузки и обработки файлов:

```bash
cd web/uploader
pip install -r requirements.txt
python app.py
```

Возможности:
- Загрузка больших файлов (до 1 GB)
- WebSocket для real-time обновлений статуса
- Асинхронная обработка с uvloop
- Менеджер задач для очереди обработки
- Jinja2 шаблоны для UI
- Docker support

## Особенности проекта

### Модульная архитектура

Проект построен по модульному принципу - каждый компонент может использоваться независимо:
- CLI инструменты работают автономно через Makefile или напрямую
- Airflow DAGs можно запускать отдельно или в составе pipeline
- Веб-приложения не зависят друг от друга

### Полный цикл обработки

```
PDF документ
    ↓
[Разделение на страницы] → cli/split_pdf_to_jpeg.py
    ↓
[Разделение разворотов] → cli/split_book_layout.py
    ↓
[Удаление шумов] → cli/unpaper_img.py
    ↓
[Поворот] → cli/rotate_img.py
    ↓
[Фильтры] → cli/apply_img_filters.py
    ↓
[Стандартизация] → cli/standartize_img.py
    ↓
[OCR] → ocrmypdf с kbd моделью
    ↓
[Проверка орфографии] → spell_checker
    ↓
[Веб-интерфейс] → Django API + Viewer
```

### Обучение и улучшение модели

Проект включает полный цикл улучшения качества OCR:
1. Сбор и очистка текстовых данных (notebooks/00_*.ipynb)
2. Генерация синтетических обучающих изображений (dags/00_image_generate.py)
3. Обучение Tesseract модели (dags/00_train_tesseract.py)
4. Сравнение результатов разных версий (dags/02_langs_compare.py)
5. Итеративное улучшение на основе метрик

### Spell Checker для кабардино-черкесского языка

Специализированный spell checker с:
- Поддержкой словарей и частотных списков
- Ahocorasick и Marisa-Trie для быстрого поиска
- Нечеткое сравнение через fuzzywuzzy
- Контекстный анализ через NLTK

## Roadmap

См. [docs/todo.md](docs/todo.md) для детального плана развития проекта.

Основные направления:
- ✅ Базовая обработка изображений и OCR
- ✅ Обучение кастомной модели Tesseract
- ✅ Сравнение качества языковых моделей
- 🔄 Оптимизация шрифтов для лучшего распознавания
- 🔄 Расширение словаря через анализ результатов OCR
- 📋 Улучшение .hocr файлов с графиками и bounding boxes
- 📋 Метрики качества на уровне слов
- 📋 Web интерфейс для вычитки и коррекции

## Вклад в проект

Мы приветствуем вклад в развитие проекта! Пожалуйста:

1. Создайте форк репозитория
2. Создайте ветку для вашей функции (`git checkout -b feature/amazing-feature`)
3. Закоммитьте изменения (`git commit -m 'Add amazing feature'`)
4. Отправьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

## Лицензия

Этот проект распространяется под лицензией [указать лицензию].

## Благодарности

- Tesseract OCR за движок распознавания текста
- OCRmyPDF за удобную обертку над Tesseract
- Сообщество разработчиков инструментов обработки изображений

## Контакты

Если у вас есть вопросы или предложения, пожалуйста, создайте [issue](https://github.com/yourusername/zbze_ocr/issues).

---

**Примечание:** Этот проект оптимизирован для работы с документами на кабардино-черкесском языке, но может быть адаптирован для других языков с соответствующими моделями Tesseract.
